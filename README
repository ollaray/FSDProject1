#This application is prototype of a main project. 
#It demonstrates the usage of the command line in persisting data, creating files, reading data from files and deleting files and its contents.
The Language used is Java version 8. 
